# TailAdmin Adapter

This folder intentionally contains no copied TailAdmin template pages.

SpeakPath admin pages must depend on `src/app/admin` wrapper components.
TailAdmin-inspired tokens and layout values live behind those wrappers.
If licensed TailAdmin assets are added later, adapt them here first.
