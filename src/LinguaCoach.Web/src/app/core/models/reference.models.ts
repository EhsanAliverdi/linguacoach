export interface LanguagePairDto {
  id: string;
  sourceCode: string;
  sourceName: string;
  targetCode: string;
  targetName: string;
}

export interface LearningTrackDto {
  id: string;
  name: string;
  description: string;
}

export interface CareerProfileDto {
  id: string;
  name: string;
  description: string;
}
