namespace LinguaCoach.Domain.Enums;

/// <summary>Phase H3 — a <see cref="Entities.LessonResourceLink"/>'s role on its Lesson:
/// the resource the explanation is primarily about, versus a supporting/supplementary reference.</summary>
public enum LessonResourceRole
{
    Primary = 0,
    Supporting = 1
}
